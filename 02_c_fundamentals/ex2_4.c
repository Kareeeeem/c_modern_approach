#include <stdio.h>

int main(void)
{
    int a, b, c;
    float d, e, f;
    /* intentionally leave variables uninitialized */

    printf("ints %d, %d, %d\n", a, b, c);
    printf("floats %f, %f, %f\n", d, e, f);
    return 0;
}
